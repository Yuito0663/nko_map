# 🗺️ NKO Map - Карта добрых дел

Интерактивная карта некоммерческих организаций в городах присутствия Росатома. Платформа для волонтеров и НКО, позволяющая находить организации, участвовать в добрых делах и добавлять новые инициативы.

## 🌟 Особенности

### 🗺️ Основной функционал
- **Интерактивная карта** с организациями using Leaflet
- **Поиск и фильтрация** по городам и категориям
- **Детальная информация** о каждой организации
- **Адаптивный дизайн** для всех устройств

### 👤 Пользовательская система
- **Регистрация и авторизация** с JWT
- **Личный кабинет** с статистикой
- **Добавление новых организаций**
- **Отслеживание статуса модерации**

### 👑 Административные функции
- **Панель модерации** организаций
- **Статистика платформы**
- **Управление пользователями**
- **Одобрение/отклонение заявок**

## 🚀 Технологии

### Frontend
- **HTML5** - семантическая верстка
- **CSS3** - адаптивные стили с CSS Variables
- **Vanilla JavaScript (ES6+)** - клиентская логика
- **Leaflet** - интерактивные карты
- **Font Awesome** - иконки
- **OpenStreetMap** - тайлы карт

### Backend
- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **PostgreSQL/MySQL** - база данных
- **Sequelize ORM** - работа с БД
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей

### Безопасность
- **Helmet** - защита заголовков
- **CORS** - кросс-доменные запросы
- **Rate Limiting** - ограничение запросов
- **JWT Tokens** - безопасная аутентификация

## 📦 Установка и запуск

### Предварительные требования
- Node.js 18+
- PostgreSQL или MySQL
- npm или yarn

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/nko_map.git
cd nko_map
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка окружения
Создайте файл `.env` в корневой директории:
```env
# Server
NODE_ENV=production
PORT=10000

# Database
DATABASE_URL=postgresql://user:pass@host:5432/dbname
# или для MySQL
DB_NAME=nkomap
DB_USER=username
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3306

# JWT
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRE=24h

# Frontend
FRONTEND_URL=https://your-frontend-domain.com
```

### 4. Настройка базы данных
Выполните SQL скрипт для создания таблиц:
```bash
psql -d your_database -f database/setup.sql
# или для MySQL
mysql -u username -p your_database < database/setup.sql
```

### 5. Запуск приложения
```bash
# Development
npm run dev

# Production
npm start
```

### 6. Создание администратора
```bash
npm run create-admin
```

## 🏗️ Структура проекта

```
nko_map/
├── config/
│   └── database.js          # Конфигурация БД
├── models/
│   ├── User.js              # Модель пользователя
│   └── NPO.js               # Модель организации
├── middleware/
│   └── auth.js              # Аутентификация
├── routes/
│   ├── auth.js              # Авторизация
│   ├── npo.js               # Организации
│   ├── profile.js           # Профиль пользователя
│   └── admin.js             # Админ-панель
├── public/
│   ├── index.html           # Главная страница
│   ├── style.css            # Стили
│   └── script.js            # Клиентский код
├── database/
│   └── setup.sql            # SQL для создания БД
├── server.js                # Точка входа
└── package.json
```

## 🎯 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Организации
- `GET /api/npo` - Получить все организации
- `POST /api/npo` - Создать организацию

### Профиль
- `GET /api/profile` - Профиль пользователя
- `PUT /api/profile` - Обновить профиль
- `GET /api/profile/npos` - Организации пользователя
- `GET /api/profile/stats` - Статистика пользователя

### Администратор
- `GET /api/admin/npos` - Организации на модерации
- `PATCH /api/admin/npos/:id/approve` - Одобрить организацию
- `PATCH /api/admin/npos/:id/reject` - Отклонить организацию
- `GET /api/admin/stats` - Статистика платформы
- `GET /api/admin/users` - Пользователи системы

## 🏙️ Поддерживаемые города

Проект охватывает города присутствия Росатома:
- Ангарск, Байкальск, Балаково, Билибино
- Волгодонск, Глазов, Десногорск, Димитровград
- Железногорск, Заречный, Зеленогорск
- И другие города атомной отрасли

## 📊 Категории организаций

- 🌿 Экология
- 🐾 Помощь животным
- 👥 Социальная поддержка
- 📚 Образование
- 🎭 Культура
- ⚽ Спорт
- 🏥 Здравоохранение
- 🔗 Другое

## 🚀 Деплой

### На Render.com
1. Подключите репозиторий к Render
2. Настройте environment variables
3. Автоматический деплой при push в main

Конфигурация для Render в `render.yaml`:
```yaml
services:
  - type: web
    name: nko-map
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
```

## 👥 Разработка

### Скрипты package.json
```json
{
  "start": "node server.js",
  "dev": "nodemon server.js",
  "build": "echo 'No build step needed'",
  "render-build": "npm install",
  "create-admin": "node scripts/createAdmin.js"
}
```

### Основные зависимости
```json
{
  "express": "^4.18.2",
  "pg": "^8.11.3",
  "sequelize": "^6.33.0",
  "bcryptjs": "^2.4.3",
  "jsonwebtoken": "^9.0.2",
  "cors": "^2.8.5",
  "helmet": "^7.0.0"
}
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature branch: `git checkout -b feature/amazing-feature`
3. Коммитьте изменения: `git commit -m 'Add amazing feature'`
4. Push в ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

## 📞 Контакты

По вопросам сотрудничества и поддержки:
- Email: support@nkomap.ru
- Телефон: +7 (XXX) XXX-XX-XX

---

**NKO Map** - Создаем сообщество добра вместе! 🌟
